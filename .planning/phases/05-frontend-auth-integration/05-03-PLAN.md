---
phase: 05-frontend-auth-integration
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-02"
files_modified:
  - frontend/SinodikApp.jsx
autonomous: true
requirements:
  - FRNT-01
  - FRNT-04
  - FRNT-07
must_haves:
  truths:
    - "Unauthenticated user sees form and header button 'Войти' (no other tabs)"
    - "Clicking Войти opens one modal: step 1 email, step 2 OTP; errors at top; no 'code sent' hint; no Back on OTP"
    - "After successful login token and user are stored; modal closes; user sees Выйти and Мои заказы (non-admin) or full tabs (admin)"
    - "Non-admin user does not see CSV, Стат., БД, Сегодня, Поиск tabs — only Записка and Мои заказы"
    - "Выйти clears token immediately and shows guest view (form + Войти)"
  artifacts:
    - path: frontend/SinodikApp.jsx
      provides: login modal (email → OTP), conditional TABS and header by user/role
  key_links:
    - from: Login modal
      to: POST /auth/request-otp and POST /auth/verify-otp
      via: fetch with body { email } / { email, code }
      pattern: "request-otp|verify-otp"
    - from: visible TABS
      to: user?.role
      via: admin => full TABS; user => Записка + Мои заказы; guest => no tab bar or single view
      pattern: "role|admin"
user_setup: []
---

<objective>
Implement login modal (email → OTP in one popup, errors at top, no "code sent" hint, no Back on OTP) and conditional shell: guest sees form + "Войти"; authenticated user sees "Мои заказы" link and "Выйти"; admin sees full tabs; non-admin does not see CSV/Стат./БД/Сегодня/Поиск.

Purpose: FRNT-01 (login screen when not authenticated), FRNT-04 (CSV tab hidden for non-admin), FRNT-07 (role-based UI). Per CONTEXT: one modal two steps; terminology "Записки".
Output: Working login flow; header and tab bar depend on user state and role.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-auth-integration/05-CONTEXT.md
@.planning/phases/05-frontend-auth-integration/05-RESEARCH.md

# CONTEXT decisions: One popup, two steps (email → OTP). Errors at top of modal. Do NOT show "Код отправлен на …". No Back/Изменить email on OTP step. Logout: immediate token clear, no confirm. UI terminology: "Записки" everywhere.
@frontend/SinodikApp.jsx
</context>

<interfaces>
POST /api/v1/auth/request-otp body: { email }. 202 success; 429 rate limit; 4xx detail.
POST /api/v1/auth/verify-otp body: { email, code }. 200 { token, user }; 401 invalid/expired.
Store token in localStorage(AUTH_KEY); setToken + setUser from verify response.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Login modal (email → OTP)</name>
  <files>frontend/SinodikApp.jsx</files>
  <action>
    Add login modal state: loginOpen (boolean), loginStep ('email' | 'otp'), loginEmail (string), loginError (string). When "Войти" is clicked set loginOpen true, loginStep 'email', loginError ''. Modal content: if loginStep === 'email': show error at top (loginError), email input, submit button "Получить код". On submit: POST /auth/request-otp with { email: loginEmail.trim() }; on 429 set loginError 'Слишком много запросов'; on other 4xx set loginError from detail or 'Ошибка'; on 202 set loginStep 'otp', loginError ''. If loginStep === 'otp': show error at top, OTP code input, submit "Войти". On submit: POST /auth/verify-otp with { email: loginEmail, code }; on 401 set loginError 'Неверный или истёкший код'; on 200: localStorage.setItem(AUTH_KEY, token), setToken(token), setUser(user), set loginOpen false and clear step/email/error. Do NOT show "Код отправлен на …". Do NOT add Back button on OTP step. Use existing T styles and modal overlay (e.g. fixed overlay, card in center). Close modal on overlay click or X if desired.
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_auth_routes.py -v -x</automated>
  </verify>
  <done>Login modal opens on Войти; email submit requests OTP; OTP submit verifies and stores token/user and closes modal; errors at top; no code-sent hint, no Back.</done>
</task>

<task type="auto">
  <name>Task 2: Conditional header and tabs by auth and role</name>
  <files>frontend/SinodikApp.jsx</files>
  <action>
    Build visible tabs from user state. Define: when !user (guest) show only main content (form Записка) and in header show "Войти" button that opens login modal. When user (authenticated): if user.role === 'admin' show full TABS (Сегодня, Поиск, Записка, CSV, Стат., БД); otherwise show only two entries: Записка and "Мои заказы" (use same Tab component; id e.g. 'add', 'myOrders'). In header when authenticated show link "Мои заказы" (or tab switch) and button "Выйти". On "Выйти" click: localStorage.removeItem(AUTH_KEY), setToken(null), setUser(null) — no confirm dialog. Ensure guest sees only form + header with Войти; non-admin user sees form + Мои заказы + Выйти (tab bar with Записка and Мои заказы only); admin sees full tab bar. Use "Записки" in labels where applicable (e.g. Мои заказы can stay as "Мои заказы" per CONTEXT "Мои заказы" link, but section title can be Записки). CONTEXT: "Терминология в UI: везде Записки" — use "Записки" for the tab/section that lists user's orders if you add a label.
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_auth_routes.py tests/test_orders_auth.py -v -x</automated>
  </verify>
  <done>Guest: form + Войти. User: Записка + Мои заказы + Выйти. Admin: full TABS + Выйти. CSV/Стат./БД/Сегодня/Поиск hidden for non-admin. Выйти clears token and shows guest view.</done>
</task>

</tasks>

<verification>
- Backend auth and orders tests pass.
- Manual: open app unauthenticated → Войти opens modal; complete email→OTP → see Выйти and Мои заказы; logout → guest view. Login as admin → all tabs visible; as user → only Записка and Мои заказы.
</verification>

<success_criteria>
- Login modal: two steps, errors at top, no code-sent hint, no Back on OTP.
- Conditional tabs and header by user and user.role; CSV/Стат./БД/Сегодня/Поиск only for admin.
- Выйти clears token and shows form + Войти.
</success_criteria>

<output>
After completion, create .planning/phases/05-frontend-auth-integration/05-03-SUMMARY.md
</output>
