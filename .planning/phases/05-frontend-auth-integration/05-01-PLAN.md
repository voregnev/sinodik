---
phase: 05-frontend-auth-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/query_service.py
  - tests/test_names_by_user_auth.py
autonomous: true
requirements:
  - FRNT-02
must_haves:
  truths:
    - "GET /api/v1/names/by-user returns order_id for each commemoration so frontend can group by order"
  artifacts:
    - path: app/services/query_service.py
      provides: get_by_user with order_id in select and returned dict
      contains: "order_id"
    - path: tests/test_names_by_user_auth.py
      provides: assertion that by-user response includes order_id in commemorations
  key_links:
    - from: app/api/routes/names.py
      to: get_by_user
      via: results passed to response
      pattern: "get_by_user"
user_setup: []
---

<objective>
Add order_id to get_by_user so "Мои заказы" can group commemorations by order and show one card per order (type, period, expiry, names/count).

Purpose: Frontend needs order_id in each commemoration to build order-grouped cards. Backend currently does not return it.
Output: query_service.get_by_user returns order_id per row; GET /api/v1/names/by-user response includes order_id in each commemoration; test coverage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-auth-integration/05-RESEARCH.md

# Backend: get_by_user currently selects Commemoration fields and Person.canonical_name; joins Order for filter only. Add Order.id as order_id to select and to returned dict.
@app/services/query_service.py
@app/api/routes/names.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add order_id to get_by_user</name>
  <files>app/services/query_service.py</files>
  <action>
    In get_by_user(): add Order.id.label("order_id") to the select() (Order is already joined). In the list comprehension that builds the returned dicts, add "order_id": r.order_id for each row. Do not change filter, order_by, or active_only logic. Commemoration has order_id FK; the join is .join(Order, Order.id == Commemoration.order_id), so selecting Order.id is correct.
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_names_by_user_auth.py -v -x</automated>
  </verify>
  <done>GET /api/v1/names/by-user response commemorations array contains order_id for each item; existing by-user tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Assert order_id in by-user response in tests</name>
  <files>tests/test_names_by_user_auth.py</files>
  <action>
    Add or extend a test that verifies the by-user response shape: when get_by_user is mocked to return at least one dict, the response body "commemorations" items include the key "order_id". Use the existing pattern (patch api.routes.names.get_by_user, return_value=[{...}]) and assert "order_id" in response["commemorations"][0].
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_names_by_user_auth.py -v -x</automated>
  </verify>
  <done>Test passes; by-user contract explicitly asserts order_id presence.</done>
</task>

</tasks>

<verification>
- Run: docker compose run --rm api pytest tests/test_names_by_user_auth.py tests/test_orders.py -v
- Manually: GET /api/v1/names/by-user with valid JWT returns commemorations with order_id in each object.
</verification>

<success_criteria>
- get_by_user returns list of dicts each containing order_id.
- GET /api/v1/names/by-user returns { user_email, commemorations: [{ ..., order_id }, ...], count }; tests pass.
</success_criteria>

<output>
After completion, create .planning/phases/05-frontend-auth-integration/05-01-SUMMARY.md
</output>
