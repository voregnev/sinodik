---
phase: 05-frontend-auth-integration
plan: "04"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - frontend/SinodikApp.jsx
autonomous: true
requirements:
  - FRNT-02
  - FRNT-03
must_haves:
  truths:
    - "Authenticated user sees 'Мои заказы' screen with list of their orders as cards (type, period, expiry, names/count)"
    - "Cards are grouped by order (one card per order using order_id from by-user response)"
    - "Empty state is empty list (no extra text per CONTEXT)"
    - "Admin in БД tab sees section 'Податели' with list of submitters and link to orders filtered by that user"
  artifacts:
    - path: frontend/SinodikApp.jsx
      provides: MyOrdersPage (or equivalent) and Податели section in БД tab
  key_links:
    - from: Мои заказы screen
      to: GET /orders and GET /names/by-user?active_only=false
      via: fetch then group by order_id
      pattern: "orders|by-user"
    - from: Податели section
      to: GET /admin/users
      via: list users then filter orders by user_email when selected
      pattern: "admin/users"
user_setup: []
---

<objective>
Implement "Мои заказы" screen (user's orders as cards grouped by order_id with type, period, expiry, names/count) and admin "Податели" section in БД tab (list of users from GET /admin/users; link to orders filtered by selected submitter).

Purpose: FRNT-02 (My Orders tab with linked commemorations), FRNT-03 (admin sees all tabs and Податели in БД). Depends on 05-01 (order_id in by-user) and 05-02 (auth state).
Output: MyOrders screen when tab is "myOrders"; БД tab for admin includes Податели block with user list and order filter.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-auth-integration/05-CONTEXT.md
@.planning/phases/05-frontend-auth-integration/05-RESEARCH.md

# 05-01 adds order_id to each comm in GET /names/by-user. GET /orders returns { id, user_email, ordered_at, ... }. Group comms by order_id; for each order show one card. CONTEXT: card has type, period, expiry, names or count; empty state = empty list. Податели: section in БД tab, list подателей (GET /admin/users), link to orders of selected user (filter orders by user_email in frontend).
@frontend/SinodikApp.jsx
@app/api/routes/names.py
@app/services/query_service.py
</context>

<interfaces>
GET /api/v1/orders: 200 → [{ id, user_email, source_channel, ordered_at, created_at, ... }]. User sees own; admin sees all.
GET /api/v1/names/by-user?active_only=false: 200 → { user_email, commemorations: [{ commemoration_id, order_id, order_type, period_type, expires_at, canonical_name, ... }], count }.
GET /api/v1/admin/users: 200 → [{ id, email, role, is_active, created_at, orders_count, active_commemoration_count }]. Admin only.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Мои заказы screen</name>
  <files>frontend/SinodikApp.jsx</files>
  <action>
    Add a screen for tab "myOrders" (Мои заказы): fetch GET /orders and GET /names/by-user?active_only=false (both require auth; use existing api() which adds Bearer). Group commemorations by order_id (each comm has order_id from 05-01). For each order in the orders list, find comms with that order_id; build one card per order showing: order type (здравие/упокоение), period (год/полгода/сорокоуст/разовое), expiry date (from first comm or max expires_at in group), and names list or count of names. Use existing T, card styling consistent with app. Empty state: if no orders or no comms, show empty list (no "Пока нет записок" text per CONTEXT). Label the section/screen with "Записки" terminology where appropriate. Ensure this screen is shown when user (non-admin) selects Мои заказы tab from 05-03.
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_orders_auth.py tests/test_names_by_user_auth.py -v -x</automated>
  </verify>
  <done>Мои заказы shows order-grouped cards with type, period, expiry, names/count; empty state is empty list.</done>
</task>

<task type="auto">
  <name>Task 2: Податели section in БД tab</name>
  <files>frontend/SinodikApp.jsx</files>
  <action>
    In the БД tab (DbManagePage or equivalent), when user.role === 'admin', add a section titled "Податели" (not "Пользователи"). Fetch GET /admin/users to get list of users (id, email, role, orders_count, active_commemoration_count). Render list of подателей (e.g. one row per user with email and counts). Each row has a link or action "Заказы" that shows orders for that user: either switch to a subview that lists orders filtered by user_email, or expand to show orders. Admin already receives all orders from GET /orders; filter that list by selected user_email (order.user_email === selectedEmail) to show "orders of this submitter". Use "Податели" as the section heading per CONTEXT. No separate "Admin" tab — admin sees full tabs and Податели is inside БД tab.
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_auth_routes.py -v -x</automated>
  </verify>
  <done>Admin sees Податели section in БД tab; list of users with link to orders filtered by that user.</done>
</task>

</tasks>

<verification>
- Backend tests for orders and names by-user pass.
- Manual: login as user → Мои заказы shows cards grouped by order; login as admin → БД tab shows Податели and filtering by submitter shows their orders.
</verification>

<success_criteria>
- Мои заказы: GET /orders + GET /names/by-user, group by order_id, one card per order (type, period, expiry, names/count); empty = empty list.
- БД tab for admin: section "Податели", list from GET /admin/users, link to orders by selected user (filter by user_email).
</success_criteria>

<output>
After completion, create .planning/phases/05-frontend-auth-integration/05-04-SUMMARY.md
</output>
