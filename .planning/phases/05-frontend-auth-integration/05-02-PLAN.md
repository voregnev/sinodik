---
phase: 05-frontend-auth-integration
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/SinodikApp.jsx
autonomous: true
requirements:
  - FRNT-05
  - FRNT-06
  - FRNT-07
must_haves:
  truths:
    - "JWT is stored in localStorage and sent as Authorization Bearer on every request when token is set"
    - "On 401 response token is cleared and user state set to null (guest view)"
    - "On load, if token exists GET /auth/me is called to set user (or clear token on 401)"
    - "Anonymous POST /orders works without token (no Bearer added when token absent)"
  artifacts:
    - path: frontend/SinodikApp.jsx
      provides: auth state (token, user), auth ref, fetch wrapper with Bearer and 401 handling
      min_lines: 1
  key_links:
    - from: frontend/SinodikApp.jsx
      to: /api/v1/auth/me
      via: GET on mount when token in localStorage
      pattern: "auth/me"
    - from: api()/apiOrThrow()
      to: Authorization Bearer + 401 clear
      via: headers and response check
      pattern: "Authorization|401"
user_setup: []
---

<objective>
Add auth state (token, user) and a single fetch wrapper that adds Authorization: Bearer when token is present and on 401 clears token and sets user to null. Hydrate user on load via GET /auth/me.

Purpose: FRNT-05 (JWT in localStorage, Bearer on requests), FRNT-06 (anonymous can submit — no Bearer when no token), FRNT-07 (role from /me for conditional UI later). No UI changes yet — only state and fetch.
Output: SinodikApp has token/user state and localStorage key sinodik_token; api/apiOrThrow add Bearer and handle 401; mount effect calls /auth/me when token exists.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-auth-integration/05-CONTEXT.md
@.planning/phases/05-frontend-auth-integration/05-RESEARCH.md

# Existing: api() and apiOrThrow() at top of SinodikApp.jsx use fetch with API base, no Authorization. All pages use these helpers. Auth routes: POST /auth/request-otp, POST /auth/verify-otp, GET /auth/me (prefix /api/v1 in main.py).
@frontend/SinodikApp.jsx
</context>

<interfaces>
API base: const API = "/api/v1"
Auth endpoints: POST /api/v1/auth/request-otp, POST /api/v1/auth/verify-otp, GET /api/v1/auth/me
GET /auth/me returns 200 { id, email, role, is_active } or 401.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Auth state and hydrate on mount</name>
  <files>frontend/SinodikApp.jsx</files>
  <action>
    In SinodikApp (export default function): add useState for token (string | null) and user (object | null). Define AUTH_KEY = "sinodik_token". Add useEffect on mount: read localStorage.getItem(AUTH_KEY). If no token, setUser(null) and return. If token present, call fetch(API + "/auth/me", { headers: { Authorization: "Bearer " + token } }). If response 401: removeItem(AUTH_KEY), setToken(null), setUser(null), return. If ok, res.json().then(data => { setToken(token); setUser(data); }). Catch: setUser(null). Do not add login UI yet — only state and hydration.
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_auth_routes.py tests/test_orders_auth.py -v -x</automated>
  </verify>
  <done>App has token and user state; on load with valid token in localStorage, user is set from /auth/me; on 401 token and user cleared.</done>
</task>

<task type="auto">
  <name>Task 2: Fetch wrapper with Bearer and 401 handling</name>
  <files>frontend/SinodikApp.jsx</files>
  <action>
    Introduce an auth ref: e.g. const authRef = { current: null }. In SinodikApp, in a useEffect (or at start of render), set authRef.current = { token, setToken, setUser } so the ref is always current. Replace the implementation of api() and apiOrThrow() so they: (1) build headers including Content-Type and, when authRef.current?.token is set, Authorization: "Bearer " + authRef.current.token; (2) after fetch, if res.status === 401 then localStorage.removeItem(AUTH_KEY), authRef.current?.setToken?.(null), authRef.current?.setUser?.(null); then in api() return null (and in apiOrThrow throw). Use the same fetch URL pattern (API + path). All existing call sites keep calling api() and apiOrThrow() — no signature change. When token is null (guest), no Bearer header is added, so anonymous POST /orders continues to work (FRNT-06).
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_auth_routes.py tests/test_orders_auth.py -v -x</automated>
  </verify>
  <done>All API calls add Bearer when token present; 401 clears token and user; guest requests send no Authorization.</done>
</task>

</tasks>

<verification>
- Backend tests for auth and orders still pass (no regression).
- Manual: open app with no token → user stays null; add token in localStorage and refresh → user set from /me; invalid token → 401 and user cleared.
</verification>

<success_criteria>
- token and user state exist; localStorage key sinodik_token used.
- api/apiOrThrow add Authorization Bearer when token present; on 401 clear token and user.
- Mount effect: GET /auth/me when token present; 401 → clear.
- Anonymous requests (no token) do not send Bearer.
</success_criteria>

<output>
After completion, create .planning/phases/05-frontend-auth-integration/05-02-SUMMARY.md
</output>
