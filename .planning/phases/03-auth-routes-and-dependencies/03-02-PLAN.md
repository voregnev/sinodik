---
phase: 03-auth-routes-and-dependencies
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app/api/routes/auth.py
  - app/main.py
  - tests/test_auth_routes.py
autonomous: true
requirements:
  - USER-03
user_setup: []

must_haves:
  truths:
    - "POST /api/v1/auth/request-otp accepts email and returns 202; OTP delivery or dev fallback occurs"
    - "POST /api/v1/auth/verify-otp with valid code returns 200 with token and user; invalid/expired returns 401"
    - "GET /api/v1/auth/me with valid JWT returns 200 with id, email, role, is_active; missing or expired token returns 401"
    - "User can log out by discarding JWT client-side; GET /auth/me rejects expired or missing tokens (USER-03)"
  artifacts:
    - path: app/api/routes/auth.py
      provides: Auth HTTP endpoints
      contains: "request-otp", "verify-otp", "me"
    - path: app/main.py
      provides: Auth router registration
      pattern: "auth.*router|include_router.*auth"
    - path: tests/test_auth_routes.py
      provides: Integration tests for auth API
      min_lines: 50
  key_links:
    - from: app/api/routes/auth.py
      to: app/services/auth_service
      via: request_otp, verify_otp
      pattern: "request_otp|verify_otp"
    - from: app/api/routes/auth.py
      to: app/api/deps
      via: get_current_user for GET /me
      pattern: "get_current_user"
    - from: app/main.py
      to: app/api/routes/auth
      via: include_router
      pattern: "auth\\.router|auth\\.router"
---

<objective>
Expose auth over HTTP: POST request-otp, POST verify-otp, GET me; register auth router; add integration tests so phase success criteria and USER-03 are verified automatically.

Purpose: Auth API is reachable over HTTP; logout is client-side discard of JWT with /auth/me rejecting missing/expired tokens.
Output: app/api/routes/auth.py, main.py updated, tests/test_auth_routes.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth-routes-and-dependencies/03-CONTEXT.md
@.planning/phases/03-auth-routes-and-dependencies/03-RESEARCH.md
@.planning/phases/03-auth-routes-and-dependencies/03-01-PLAN.md

# 03-CONTEXT locked: /api/v1/auth/ prefix; request-otp 202 + message (and dev_otp_code when fallback), 429 rate limit, 400 invalid email; verify-otp 200 { token, user: { email, role } }, 401 "Invalid or expired code"; GET /me 200 { id, email, role, is_active }, 401 missing/invalid/expired.
# auth_service.request_otp: raises ValueError for invalid email; returns { success, message } or { success, message, dev_otp_code }; "Rate limit" in message for 429.
# auth_service.verify_otp: returns { token, user: { id, email, role } } or None.
</context>

<interfaces>
<!-- From app/services/auth_service -->
async def request_otp(email: str, db_session: AsyncSession) -> Dict[str, Any]  # raises ValueError on invalid email; returns success, message; optional dev_otp_code
async def verify_otp(email: str, code: str, db_session: AsyncSession) -> Optional[Dict]  # None or { token, user: { id, email, role } }
<!-- From app/api/deps (plan 01) -->
async def get_current_user(...) -> User
async def require_admin(...) -> User
<!-- From app/database -->
get_db
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create app/api/routes/auth.py with three endpoints</name>
  <files>app/api/routes/auth.py</files>
  <action>
Create app/api/routes/auth.py (APIRouter, prefix not applied here — main.py uses prefix="/api/v1"):

- POST /auth/request-otp (status_code=202): Body Pydantic model with email: str. Call request_otp(body.email, db). On ValueError (invalid email) raise HTTPException(400). If not result["success"]: if "Rate limit" in result.get("message", ""): raise HTTPException(429, detail=result["message"]); else raise HTTPException(400, detail=result.get("message", "Bad request")). Build response dict: {"message": result["message"]}; if "dev_otp_code" in result add response["dev_otp_code"] = result["dev_otp_code"]. Return response.

- POST /auth/verify-otp: Body with email: str, code: str. Call verify_otp(body.email, body.code, db). If result is None raise HTTPException(401, detail="Invalid or expired code"). Return {"token": result["token"], "user": result["user"]} (200).

- GET /auth/me: Depends(get_current_user). Return {"id": current_user.id, "email": current_user.email, "role": current_user.role, "is_active": current_user.is_active}.

Imports: from fastapi import APIRouter, Depends, HTTPException; from database import get_db; from sqlalchemy.ext.asyncio import AsyncSession; from services.auth_service import request_otp, verify_otp; from api.deps import get_current_user; from models.models import User. Use same import style as app/api/routes/orders.py (database, models, services).
  </action>
  <verify>
    <automated>cd /Users/kolia/sinodic && docker compose run --rm api python -c "from api.routes import auth; assert len([r for r in auth.router.routes if hasattr(r,'path')]) >= 3"</automated>
  </verify>
  <done>Three endpoints implemented; request-otp maps to 202/429/400; verify-otp to 200/401; GET me returns user or 401 via deps.</done>
</task>

<task type="auto">
  <name>Task 2: Register auth router in main.py</name>
  <files>app/main.py</files>
  <action>
In app/main.py: add to imports: from api.routes import ..., auth (add auth). Add: app.include_router(auth.router, prefix="/api/v1", tags=["auth"]). Place with other include_router calls.
  </action>
  <verify>
    <automated>cd /Users/kolia/sinodic && docker compose run --rm api python -c "from main import app; r=[x for x in app.routes if hasattr(x,'path') and 'auth' in str(x.path)]; assert len(r)>=1"</automated>
  </verify>
  <done>Auth router registered under /api/v1; GET /api/v1/auth/me and POST request-otp/verify-otp are reachable.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests/test_auth_routes.py for phase criteria and USER-03</name>
  <files>tests/test_auth_routes.py</files>
  <action>
Create tests/test_auth_routes.py using pytest and FastAPI TestClient (from fastapi.testclient import TestClient; from main import app; client = TestClient(app)).

Tests to implement:
1. request_otp: valid email returns 202 and body has "message"; invalid email (e.g. "not-an-email") returns 400.
2. request_otp rate limit: send 5+ request-otp for same email in quick succession; expect 429 on the over-limit request (if auth_service and DB support it; otherwise skip or mock).
3. verify_otp: after request_otp, use the code (from dev_otp_code when SINODIK_OTP_PLAINTEXT_FALLBACK is true, or mock) to POST verify-otp; expect 200 with "token" and "user" with email, role. Invalid code expect 401 with detail "Invalid or expired code".
4. GET /api/v1/auth/me without Authorization header returns 401 (USER-03: logout = discard JWT; /me rejects missing token).
5. GET /api/v1/auth/me with valid Bearer token (from verify_otp response) returns 200 with id, email, role, is_active.
6. GET /api/v1/auth/me with expired or malformed token returns 401.

Use async pytest (pytest with asyncio_mode = auto per project). Create OTP in test via auth_service or by enabling plaintext fallback in env so tests can get dev_otp_code from request-otp response when fallback is on.
  </action>
  <verify>
    <automated>cd /Users/kolia/sinodic && docker compose run --rm api pytest tests/test_auth_routes.py -v -x</automated>
  </verify>
  <done>All phase success criteria and USER-03 covered by automated tests; pytest passes.</done>
</task>

</tasks>

<verification>
- POST /auth/request-otp 202 for valid email; 429 on rate limit; 400 on invalid email.
- POST /auth/verify-otp 200 with token and user; 401 for invalid/expired code.
- GET /auth/me 200 with user when valid JWT; 401 when missing or expired (USER-03).
- Full suite: docker compose run --rm api pytest tests/ -v
</verification>

<success_criteria>
- Auth API is reachable over HTTP; OTP request and verify endpoints are live; GET /auth/me works with deps; USER-03 satisfied (client discards JWT, /auth/me rejects missing/expired).
</success_criteria>

<output>
After completion, create .planning/phases/03-auth-routes-and-dependencies/03-02-SUMMARY.md
</output>
