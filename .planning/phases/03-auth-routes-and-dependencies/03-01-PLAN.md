---
phase: 03-auth-routes-and-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/deps.py
  - tests/test_auth_deps.py
autonomous: true
requirements:
  - USER-03
user_setup: []

must_haves:
  truths:
    - "JWT verification dependency exists and returns 401 for missing or invalid token"
    - "Role-checking dependency exists and returns 403 when caller is not admin"
  artifacts:
    - path: app/api/deps.py
      provides: get_current_user and require_admin dependencies
      contains: "get_current_user", "require_admin", "HTTPBearer"
    - path: tests/test_auth_deps.py
      provides: Import and export verification for deps
      min_lines: 10
  key_links:
    - from: app/api/deps.py
      to: config.settings
      via: jwt_secret for PyJWT decode
      pattern: "jwt\\.decode|settings\\.jwt_secret"
    - from: app/api/deps.py
      to: database.get_db
      via: Depends(get_db)
      pattern: "get_db"
    - from: app/api/deps.py
      to: User model
      via: load by email from payload["sub"]
      pattern: "User\\.email.*sub"
---

<objective>
Add shared API dependencies for JWT verification and admin role check so any route can protect endpoints by using get_current_user and require_admin.

Purpose: Phase 4 and later routes need to depend on these without duplicating JWT logic.
Output: app/api/deps.py with get_current_user and require_admin; tests/test_auth_deps.py for import/export verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth-routes-and-dependencies/03-CONTEXT.md
@.planning/phases/03-auth-routes-and-dependencies/03-RESEARCH.md

# Auth service (Phase 2) — JWT payload and User shape
# app/services/auth_service.py: create_jwt_token uses payload["sub"]=email, "role"; User has email, role, is_active, id
# Existing routes use: from database import get_db; from models import ... (User is in models.models, not in models.__all__)
</context>

<interfaces>
<!-- From app/services/auth_service.py -->
create_jwt_token(email, role) -> str  # payload: sub=email, role, exp, iat; algorithm HS256
<!-- From app/models/models.py -->
class User: id, email, role, is_active, created_at
<!-- From app/database.py -->
async def get_db() -> AsyncSession
<!-- From app/config -->
settings.jwt_secret, settings.jwt_ttl_days
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create app/api/deps.py and tests/test_auth_deps.py</name>
  <files>app/api/deps.py, tests/test_auth_deps.py</files>
  <action>
Create app/api/deps.py with:
- Use fastapi.security.HTTPBearer(auto_error=False) so missing token does not auto-raise; we raise 401 explicitly.
- get_current_user(credentials: HTTPAuthorizationCredentials | None = Depends(oauth2_scheme), db: AsyncSession = Depends(get_db)) -> User:
  - If credentials is None, raise HTTPException(401, detail="Not authenticated").
  - Decode JWT with jwt.decode(credentials.credentials, settings.jwt_secret, algorithms=["HS256"]) (PyJWT); on jwt.InvalidTokenError raise HTTPException(401, detail="Invalid or expired token").
  - email = payload.get("sub"); if not email raise 401.
  - Load user: select(User).where(User.email == email), scalar_one_or_none(); if not user or not user.is_active raise HTTPException(401, detail="User not found or inactive").
  - Return user.
- require_admin(user: User = Depends(get_current_user)) -> User: if user.role != "admin" raise HTTPException(403, detail="Admin required"); return user.

Imports: from fastapi import Depends, HTTPException, status; from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials; import jwt; from sqlalchemy import select; from sqlalchemy.ext.asyncio import AsyncSession; from config import settings; from database import get_db; from models.models import User.

Create tests/test_auth_deps.py:
- Test that get_current_user and require_admin are importable from app.api.deps and are callable (e.g. inspect.iscoroutinefunction or callable check). This verifies the module exists and exports the dependencies for use by auth routes in plan 02.
  </action>
  <verify>
    <automated>cd /Users/kolia/sinodic && docker compose run --rm api pytest tests/test_auth_deps.py -v -x</automated>
  </verify>
  <done>app/api/deps.py exists with get_current_user and require_admin; pytest tests/test_auth_deps.py passes.</done>
</task>

</tasks>

<verification>
- Module app.api.deps can be imported; get_current_user and require_admin are used by auth routes in plan 02.
- No JWT decode logic in routes — only in deps.
</verification>

<success_criteria>
- get_current_user validates Bearer JWT, loads User by email (payload["sub"]), returns 401 for missing/invalid/expired/inactive.
- require_admin depends on get_current_user and returns 403 if role != "admin".
- tests/test_auth_deps.py passes.
</success_criteria>

<output>
After completion, create .planning/phases/03-auth-routes-and-dependencies/03-01-SUMMARY.md
</output>
