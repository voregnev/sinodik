---
phase: 02-auth-service-core
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - app/services/auth_service.py
  - app/services/email_service.py
  - tests/test_auth_service.py
  - tests/test_email_service.py
autonomous: true
requirements:
  - AUTH-07
  - AUTH-08
  - AUTH-09
user_setup: []

must_haves:
  truths:
    - OTP codes expire after 10 minutes
    - OTP codes are single-use (invalidated immediately on successful verification)
    - OTP requests are rate-limited per email to prevent brute-force
  artifacts:
    - path: "app/services/auth_service.py"
      provides: "Complete OTP lifecycle management (expiry, single-use, rate limiting)"
      min_lines: 150
    - path: "app/services/email_service.py"
      provides: "Complete SMTP delivery with plaintext fallback"
      min_lines: 50
    - path: "tests/test_auth_service.py"
      provides: "Unit tests for complete auth functionality"
      exports: ["test_otp_expiry", "test_otp_single_use", "test_otp_rate_limiting"]
    - path: "tests/test_email_service.py"
      provides: "Unit tests for email delivery"
      exports: ["test_smtp_fallback"]
  key_links:
    - from: "app/services/auth_service.py"
      to: "app/services/email_service.py"
      via: "OTP delivery"
      pattern: "send_otp_email"
---

<objective>
Complete the authentication service implementation by integrating auth and email services with fallback mechanisms, implementing OTP lifecycle management (expiry, single-use), and adding rate limiting functionality. This addresses the remaining AUTH-* requirements.

Purpose: Complete the full authentication flow with proper security measures, integration, and lifecycle management.
Output: Fully integrated auth and email services with comprehensive test coverage for all AUTH-* requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth-service-core/02-CONTEXT.md
@.planning/phases/02-auth-service-core/02-RESEARCH.md
@.planning/phases/02-auth-service-core/02-VALIDATION.md
@app/services/auth_service.py
@app/services/email_service.py
@app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 0: Integrate auth and email services with fallback mechanism</name>
  <files>app/services/auth_service.py, app/services/email_service.py</files>
  <action>Implement the integration between auth_service and email_service that respects the user decision about SINODIK_OTP_PLAINTEXT_FALLBACK. Specifically:
1. Update auth_service.request_otp to handle SMTP failure scenarios properly
2. When email delivery succeeds, return success without OTP in response
3. When email delivery fails and settings.otp_plaintext_fallback is True, return OTP in API response
4. When email delivery fails and settings.otp_plaintext_fallback is False, return appropriate error
5. Add proper error handling and logging for the auth->email service integration</action>
  <verify>pytest tests/test_auth_service.py::test_auth_email_integration -x && pytest tests/test_email_service.py::test_smtp_fallback -x</verify>
  <done>Auth service properly integrates with email service respecting the plaintext fallback setting</done>
</task>

<task type="auto">
  <name>Task 1: Implement OTP lifecycle and rate limiting features</name>
  <files>app/services/auth_service.py</files>
  <action>Enhance auth_service.py to include the remaining security features:
1. OTP expiry verification (codes older than 10 minutes are rejected)
2. Single-use enforcement (OTP codes marked as used after first verification)
3. Rate limiting mechanism (max 5 attempts per email within a timeframe)
4. Additional security measures like proper cleanup of expired OTPs</action>
  <verify>pytest tests/test_auth_service.py::test_otp_expiry -x && pytest tests/test_auth_service.py::test_otp_single_use -x && pytest tests/test_auth_service.py::test_otp_rate_limiting -x</verify>
  <done>All security features implemented: OTP expiry, single-use, and rate limiting</done>
</task>

</tasks>

<verification>
Run full test suite to ensure all AUTH-* requirements are covered: pytest tests/test_auth_service.py tests/test_email_service.py -v
</verification>

<success_criteria>
AUTH-07, AUTH-08, and AUTH-09 requirements are implemented as testable business logic with complete integration between auth and email services, including proper handling of the SINODIK_OTP_PLAINTEXT_FALLBACK setting.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-service-core/02-03-SUMMARY.md`
</output>