---
phase: 02-auth-service-core
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/services/email_service.py
  - tests/test_email_service.py
autonomous: true
requirements:
  - AUTH-05
  - AUTH-06
user_setup: []

must_haves:
  truths:
    - User receives the OTP via email (SMTP) when configured
    - Email delivery handles failures gracefully with appropriate fallback mechanisms
  artifacts:
    - path: "app/services/email_service.py"
      provides: "SMTP delivery with plaintext fallback"
      min_lines: 50
    - path: "tests/test_email_service.py"
      provides: "Unit tests for email delivery"
      exports: ["test_send_otp_email", "test_smtp_fallback"]
  key_links:
    - from: "app/services/email_service.py"
      to: "aiosmtplib"
      via: "async SMTP delivery"
      pattern: "aiosmtplib"
---

<objective>
Implement the email service for OTP delivery with SMTP functionality and fallback mechanisms. This provides the email delivery capability that supports the authentication flow.

Purpose: Create reliable email delivery mechanism for OTP codes with proper error handling and fallback options.
Output: email_service.py with full test coverage for OTP delivery and fallback functionality.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth-service-core/02-CONTEXT.md
@.planning/phases/02-auth-service-core/02-RESEARCH.md
@.planning/phases/02-auth-service-core/02-VALIDATION.md
@app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create email service test scaffolding</name>
  <files>tests/test_email_service.py</files>
  <action>Create initial test file with stubs for send_otp_email and smtp_fallback test to guide implementation.</action>
  <verify>python -c "import os; open('tests/test_email_service.py', 'a').close() if not os.path.exists('tests/test_email_service.py') else None; print('Test file exists')" && test -f tests/test_email_service.py</verify>
  <done>Test file exists with stubbed test function for OTP email delivery</done>
</task>

<task type="auto">
  <name>Task 1: Implement email service with SMTP and fallback</name>
  <files>app/services/email_service.py</files>
  <action>Create the email service module with the following functionality:
1. send_otp_email(email: str, otp_code: str) -> bool
   - Uses aiosmtplib for async SMTP delivery
   - Implements proper error handling for SMTP failures
   - When SMTP fails and settings.otp_plaintext_fallback is True, handle appropriately
   - When SMTP fails and plaintext fallback is disabled, raise appropriate exception
2. Proper async context manager usage for SMTP connections
3. Configurable SMTP settings from environment variables SINODIK_SMTP_HOST, SINODIK_SMTP_PORT, etc.</action>
  <verify>pytest tests/test_email_service.py::test_send_otp_email -x</verify>
  <done>Email delivery implemented with SMTP and plaintext fallback options</done>
</task>

</tasks>

<verification>
Run email service test suite to ensure AUTH-05 and AUTH-06 requirements are covered: pytest tests/test_email_service.py -v
</verification>

<success_criteria>
AUTH-05 and AUTH-06 requirements are implemented as testable email delivery functionality in email_service.py with full test coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-service-core/02-02-SUMMARY.md`
</output>