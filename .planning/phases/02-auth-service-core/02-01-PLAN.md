---
phase: 02-auth-service-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/auth_service.py
  - tests/test_auth_service.py
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup: []

must_haves:
  truths:
    - User can request a one-time code by providing their email address
    - User can verify the OTP code and receive a JWT session token
    - Account is created automatically on first successful OTP verification (no separate registration step)
  artifacts:
    - path: "app/services/auth_service.py"
      provides: "OTP generation, verification, and JWT issuance"
      min_lines: 80
    - path: "tests/test_auth_service.py"
      provides: "Unit tests for auth functionality"
      exports: ["test_request_otp", "test_verify_otp", "test_jwt_claims_structure"]
  key_links:
    - from: "app/services/auth_service.py"
      to: "app/models/models.py"
      via: "User and OtpCode ORM models"
      pattern: "User, OtpCode"
    - from: "app/services/auth_service.py"
      to: "app/config.py"
      via: "jwt_secret and jwt_ttl_days settings"
      pattern: "settings.jwt_secret, settings.jwt_ttl_days"
---

<objective>
Implement the core authentication service with basic OTP generation, verification, and JWT signing functionality. Create the foundational test scaffolding to guide implementation of AUTH-02 through AUTH-04 requirements.

Purpose: Create the foundation for authentication functionality with basic security practices and initial test coverage.
Output: Basic auth_service.py with initial test coverage for OTP request and verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth-service-core/02-CONTEXT.md
@.planning/phases/02-auth-service-core/02-RESEARCH.md
@.planning/phases/02-auth-service-core/02-VALIDATION.md
@app/models/models.py
@app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create auth service test scaffolding</name>
  <files>tests/test_auth_service.py</files>
  <action>Create initial test file with stubs for all AUTH-* requirements tests to guide implementation. Define test functions for request_otp, verify_otp, jwt_claims_structure, and basic auth functionality.</action>
  <verify>python -c "import os; open('tests/test_auth_service.py', 'a').close() if not os.path.exists('tests/test_auth_service.py') else None; print('Test file exists')" && test -f tests/test_auth_service.py</verify>
  <done>Test file exists with stubbed test functions for all AUTH-* requirements</done>
</task>

<task type="auto">
  <name>Task 1: Implement auth service core logic</name>
  <files>app/services/auth_service.py</files>
  <action>Create the auth service module with the following functionality:
1. request_otp(email: str, db_session: AsyncSession) -> dict
   - Generates 6-digit OTP using secrets.randbelow(1_000_000)
   - Stores SHA-256 hash of OTP in otp_codes table
   - Sets expires_at to 10 minutes from now
   - Returns appropriate success/error indicators
2. verify_otp(email: str, code: str, db_session: AsyncSession) -> dict | None
   - Finds unexpired, unused OTP for email using constant-time comparison (hmac.compare_digest)
   - Marks OTP as used after successful verification
   - Implements rate limiting (fail after 5 attempts)
   - Creates user account if not exists (auto-admin if email in settings.admin_emails)
   - Updates last_login_at and returns JWT with email, role, and expiry
3. Helper functions for JWT creation using PyJWT with HS256, including proper claims (sub, role, exp)</action>
  <verify>pytest tests/test_auth_service.py::test_request_otp -x</verify>
  <done>Basic OTP request and verification functionality implemented with security measures</done>
</task>

</tasks>

<verification>
Run basic test suite to ensure AUTH-02 through AUTH-04 requirements are covered: pytest tests/test_auth_service.py -v
</verification>

<success_criteria>
AUTH-02, AUTH-03, and AUTH-04 requirements are implemented as testable business logic in auth_service.py with initial test coverage for basic functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-service-core/02-01-SUMMARY.md`
</output>