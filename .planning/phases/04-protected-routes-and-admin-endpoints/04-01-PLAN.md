---
phase: 04-protected-routes-and-admin-endpoints
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_orders_auth.py
  - tests/test_names_by_user_auth.py
  - tests/test_upload_auth.py
  - tests/test_commemorations_auth.py
  - tests/test_admin_routes.py
autonomous: true
requirements:
  - USER-01
must_haves:
  truths:
    - "Test modules for Phase 4 auth and scope exist and are collectible"
  artifacts:
    - path: tests/test_orders_auth.py
      provides: "Scaffold for GET/POST orders auth and scope tests"
    - path: tests/test_names_by_user_auth.py
      provides: "Scaffold for by-user scope and admin ?email= tests"
    - path: tests/test_upload_auth.py
      provides: "Scaffold for POST /upload/csv require_admin tests"
    - path: tests/test_commemorations_auth.py
      provides: "Scaffold for GET list scope and PATCH/DELETE/bulk-update admin tests"
    - path: tests/test_admin_routes.py
      provides: "Scaffold for GET/PATCH admin/users and last-admin tests"
  key_links: []
---

<objective>
Create test file scaffolds and shared fixtures for Phase 4 so that plans 04-02–04-04 can add test cases and run targeted pytest. No production code in this plan.
Purpose: Nyquist rule — every task verify needs an automated command; Wave 0 provides the test modules.
Output: Five test files with shared fixtures (auth headers for user/admin, test client, db) and placeholder or minimal tests that pass collection.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protected-routes-and-admin-endpoints/04-CONTEXT.md
@.planning/phases/04-protected-routes-and-admin-endpoints/04-RESEARCH.md

Phase 3 established: app/api/deps.py (get_current_user, require_admin), tests use dependency_overrides or real JWT. For Phase 4 integration tests we need real JWTs: create User rows and obtain tokens via auth_service or a test helper so that 401/403 and scope can be asserted.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared fixtures and test scaffolds</name>
  <files>tests/test_orders_auth.py, tests/test_names_by_user_auth.py, tests/test_upload_auth.py, tests/test_commemorations_auth.py, tests/test_admin_routes.py</files>
  <action>
Create five test modules under tests/:

1. **Shared fixtures** (in one module, e.g. tests/conftest.py, or duplicated minimally per file): 
   - `client`: TestClient(app) from fastapi.testclient.
   - `db`: AsyncSession or sync engine/session for creating test User and Order/Commemoration rows. Use same pattern as tests/test_auth_routes.py (async app, overrides, or real DB).
   - `auth_headers_user`: dict with "Authorization": "Bearer &lt;valid_jwt_for_user&gt;" where the user has role="user". Create User in DB (or use existing fixture), then get JWT: either call auth_service.create_jwt_token(email, role) if exported, or POST /api/v1/auth/request-otp and verify-otp with a known OTP for test DB.
   - `auth_headers_admin`: same but role="admin".

   If Phase 3 tests use dependency_overrides for get_current_user, replicate that pattern here so fixtures inject a User without needing real OTP flow. Prefer real JWT from auth_service for integration tests so 401/403 on invalid token are testable.

2. **test_orders_auth.py**: Import router/app and fixtures. Add placeholder test: e.g. test_orders_list_requires_auth (GET /api/v1/orders without token returns 401). Leave room for USER-01 and ADMN-01 tests in plan 04-02.

3. **test_names_by_user_auth.py**: Placeholder test: GET /api/v1/names/by-user without token returns 401.

4. **test_upload_auth.py**: Placeholder test: POST /api/v1/upload/csv without token returns 401 (or 403 if token present but not admin — implement in 04-03).

5. **test_commemorations_auth.py**: Placeholder test: GET /api/v1/commemorations without token returns 401.

6. **test_admin_routes.py**: Placeholder test: GET /api/v1/admin/users without token returns 401.

Use pytest; follow tests/test_auth_routes.py import style (from main import app or equivalent for Docker-safe path). Conftest.py can live in tests/conftest.py for shared client/db/auth fixtures if that fits the project.
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_orders_auth.py tests/test_names_by_user_auth.py tests/test_upload_auth.py tests/test_commemorations_auth.py tests/test_admin_routes.py -v</automated>
  </verify>
  <done>All five test modules exist; pytest collects and runs placeholder tests; at least one test per file (e.g. 401 without token); shared auth fixtures available for later plans.</done>
</task>

</tasks>

<verification>
- Pytest collects all five modules without import errors.
- Placeholder tests pass (401 for protected routes without token).
</verification>

<success_criteria>
- Five test files created; conftest.py or per-file fixtures provide auth_headers_user and auth_headers_admin.
- Later plans can add test cases to these files and run pytest tests/test_*_auth.py tests/test_admin_routes.py -x.
</success_criteria>

<output>
After completion, create `.planning/phases/04-protected-routes-and-admin-endpoints/04-01-SUMMARY.md`
</output>
