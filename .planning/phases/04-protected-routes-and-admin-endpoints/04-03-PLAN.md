---
phase: 04-protected-routes-and-admin-endpoints
plan: "03"
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - app/api/routes/upload.py
  - app/api/routes/commemorations.py
  - app/services/query_service.py
  - tests/test_upload_auth.py
  - tests/test_commemorations_auth.py
autonomous: true
requirements:
  - ADMN-06
  - ADMN-07
  - ADMN-08
must_haves:
  truths:
    - "POST /upload/csv returns 403 for non-admin"
    - "GET /commemorations requires auth; user sees only own order commemorations, admin sees all"
    - "PATCH/DELETE /commemorations/{id} and POST /commemorations/bulk-update require admin"
  artifacts:
    - path: app/api/routes/upload.py
      provides: "POST /upload/csv with require_admin"
    - path: app/api/routes/commemorations.py
      provides: "GET list with get_current_user and scope; mutate with require_admin"
    - path: app/services/query_service.py
      provides: "get_commemorations with optional user_email scope"
  key_links:
    - from: app/api/routes/upload.py
      to: app/api/deps.py
      via: "Depends(require_admin)"
    - from: app/api/routes/commemorations.py
      to: app/services/query_service.py
      via: "get_commemorations(db, user_email=..., no_start_date, limit, offset)"
---

<objective>
Restrict CSV upload to admin only and protect commemorations list/mutate: GET list requires auth and scope (user = own orders' commemorations, admin = all); PATCH/DELETE by id and bulk-update are admin-only.
Purpose: ADMN-06, ADMN-07 (admin edit/delete commemorations), ADMN-08 (CSV upload admin-only).
Output: upload.py and commemorations.py guarded; query_service.get_commemorations accepts optional user_email; tests for 401/403 and scope.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-protected-routes-and-admin-endpoints/04-CONTEXT.md
@.planning/phases/04-protected-routes-and-admin-endpoints/04-RESEARCH.md

From app/api/routes/upload.py: POST /upload/csv has no auth. From app/api/routes/commemorations.py: GET /commemorations calls get_commemorations(db, no_start_date, limit, offset); PATCH/DELETE and POST bulk-update have no auth. From app/services/query_service.py: get_commemorations(db, no_start_date=False, limit=100, offset=0) — no user filter; joins Person and Order (Order.user_email available).
</context>

<interfaces>
From app/services/query_service.py:
- get_commemorations(db, no_start_date=False, limit=100, offset=0) -> list[dict]
  Extend signature to: get_commemorations(db, no_start_date=False, limit=100, offset=0, user_email: str | None = None). When user_email is set, add .where(Order.user_email == user_email) (and join Order if not already). When None, no user filter (admin view).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Require admin on POST /upload/csv</name>
  <files>app/api/routes/upload.py, tests/test_upload_auth.py</files>
  <action>
Add Depends(require_admin) to upload_csv. Import require_admin from api.deps (or deps from same package as other routes). Non-admin caller gets 403. Add tests: no token -> 401; user token -> 403; admin token -> 200/201 with valid CSV (or mock process_csv_upload to avoid real parsing).
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_upload_auth.py -v -x</automated>
  </verify>
  <done>POST /upload/csv returns 403 for non-admin (ADMN-08).</done>
</task>

<task type="auto">
  <name>Task 2: Guard and scope GET /commemorations; require_admin on mutate</name>
  <files>app/services/query_service.py, app/api/routes/commemorations.py, tests/test_commemorations_auth.py</files>
  <action>
- query_service.get_commemorations: add parameter user_email: str | None = None. When user_email is not None, add .where(Order.user_email == user_email) to the select (Order is already outerjoin'd). When None, do not filter by user (admin sees all).
- GET /commemorations: add Depends(get_current_user). effective_email = current_user.email if current_user.role != "admin" else None; for admin pass None so no filter. Call get_commemorations(db, no_start_date=no_start_date, limit=limit, offset=offset, user_email=effective_email). Same response shape.
- PATCH /commemorations/{commemoration_id}, DELETE /commemorations/{commemoration_id}, POST /commemorations/bulk-update: add Depends(require_admin). Handler logic unchanged.
- Add tests: GET without token -> 401; GET with user token -> only commemorations from orders where user_email == that user; GET with admin -> all; PATCH/DELETE/bulk-update as user -> 403; as admin -> success when resource exists (ADMN-06, ADMN-07).
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_commemorations_auth.py -v -x</automated>
  </verify>
  <done>GET /commemorations requires auth and scope; mutate endpoints admin-only.</done>
</task>

</tasks>

<verification>
- pytest tests/test_upload_auth.py tests/test_commemorations_auth.py -v passes.
</verification>

<success_criteria>
- Upload and commemorations routes guarded per CONTEXT; get_commemorations supports user_email scope; tests cover 401, 403, and scope.
</success_criteria>

<output>
After completion, create `.planning/phases/04-protected-routes-and-admin-endpoints/04-03-SUMMARY.md`
</output>
