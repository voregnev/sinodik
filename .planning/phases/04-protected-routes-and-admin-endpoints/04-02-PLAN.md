---
phase: 04-protected-routes-and-admin-endpoints
plan: "02"
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - app/api/deps.py
  - app/api/routes/names.py
  - app/api/routes/orders.py
  - tests/test_names_by_user_auth.py
  - tests/test_orders_auth.py
autonomous: true
requirements:
  - USER-01
  - ADMN-01
must_haves:
  truths:
    - "Authenticated user sees only their own data on GET /names/by-user and GET /orders"
    - "Admin sees all orders; optional ?email= on by-user for admin"
    - "POST /orders with JWT sets user_email from token; without JWT anonymous order allowed"
    - "GET /orders/{id}, PATCH/DELETE order require admin"
  artifacts:
    - path: app/api/deps.py
      provides: "get_current_user_optional (User | None)"
    - path: app/api/routes/names.py
      provides: "GET /names/by-user with get_current_user and scope"
    - path: app/api/routes/orders.py
      provides: "GET/POST/GET id/PATCH/DELETE with auth and scope"
  key_links:
    - from: app/api/routes/orders.py
      to: app/api/deps.py
      via: "Depends(get_current_user_optional) on POST, get_current_user on GET, require_admin on get/patch/delete by id"
    - from: app/api/routes/names.py
      to: app/services/query_service.py
      via: "get_by_user(db, effective_email, active_only)"
---

<objective>
Add optional-auth dependency and protect names/by-user and orders: user sees only own data, admin sees all; POST /orders links to JWT user when present else anonymous; order by-id and mutate are admin-only.
Purpose: Deliver USER-01 (user views own orders) and ADMN-01 (admin views all orders); enforce 401 on protected routes without token.
Output: get_current_user_optional in deps.py; names and orders routes guarded and scoped; tests for 401, scope, and admin-only.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-protected-routes-and-admin-endpoints/04-CONTEXT.md
@.planning/phases/04-protected-routes-and-admin-endpoints/04-RESEARCH.md

From app/api/deps.py: get_current_user(credentials, db) -> User raises 401; require_admin(user) -> User raises 403 if role != "admin". Use HTTPBearer(auto_error=False).
From app/api/routes/names.py: GET /names/by-user currently takes email=Query(..., description="Email заказчика"), active_only=Query(default=True), calls get_by_user(db, user_email=email, active_only=active_only).
From app/api/routes/orders.py: list_orders has no auth, selects Order with limit/offset; create_order uses body.user_email; get_order, update_order, delete_order have no auth.
</context>

<interfaces>
From app/api/deps.py:
- get_current_user(...) -> User
- require_admin(user: User = Depends(get_current_user)) -> User
- oauth2_scheme = HTTPBearer(auto_error=False)

From app/services/query_service.py:
- get_by_user(db, user_email, active_only) -> list (commemorations for that user_email)

From app/services/order_service.py:
- create_manual_order(db, order_type, period_type_raw, names_text, user_email=..., starts_at=..., need_receipt=...) -> list[Commemoration]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add get_current_user_optional to deps.py</name>
  <files>app/api/deps.py</files>
  <action>
Add async def get_current_user_optional(credentials, db) -> User | None. Same Bearer extraction and JWT decode as get_current_user; if credentials is None or decode fails or user not found/inactive, return None instead of raising 401. Use settings.jwt_secret and algorithms=["HS256"]. Load User by payload["sub"] (email). Do not raise; return None on any failure.
  </action>
  <verify>
    <automated>docker compose run --rm api python -c "from api.deps import get_current_user_optional; print('ok')"</automated>
  </verify>
  <done>get_current_user_optional is importable and returns None when no/invalid token.</done>
</task>

<task type="auto">
  <name>Task 2: Guard and scope GET /names/by-user</name>
  <files>app/api/routes/names.py, tests/test_names_by_user_auth.py</files>
  <action>
- Add Depends(get_current_user) to GET /names/by-user. For non-admin: do not read email from query; use current_user.email only (effective_email = current_user.email). For admin: allow optional query param email; if present use it, else use current_user.email. Call get_by_user(db, user_email=effective_email, active_only=active_only). Response shape unchanged: {"user_email": effective_email, "commemorations": results, "count": len(results)}. Remove the required Query(..., description="Email заказчика") so that email is optional (and only used for admin).
- Add tests: (1) no token -> 401; (2) user token -> returns only that user's data, no email query allowed for non-admin; (3) admin token without ?email= -> own data; (4) admin with ?email=other@x -> that user's data.
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_names_by_user_auth.py -v -x</automated>
  </verify>
  <done>GET /names/by-user requires auth; user sees only own data; admin can pass optional ?email=.</done>
</task>

<task type="auto">
  <name>Task 3: Guard and scope orders (GET list, POST optional auth, GET id / PATCH / DELETE admin)</name>
  <files>app/api/routes/orders.py, tests/test_orders_auth.py</files>
  <action>
- GET /orders: add Depends(get_current_user). Filter: for non-admin Order.user_email == current_user.email; for admin no filter. Keep limit/offset. Return same list shape.
- POST /orders: add Depends(get_current_user_optional). If current_user is not None, set link_email = current_user.email and pass to create_manual_order(..., user_email=link_email), ignoring body.user_email for linking. If current_user is None, use body.user_email as before (anonymous order per AUTH-01).
- GET /orders/{order_id}, PATCH /orders/{order_id}, DELETE /orders/{order_id}: add Depends(require_admin). Logic unchanged; only admin can access by id.
- Add tests: GET /orders without token -> 401; GET with user token -> only orders where user_email == that user; GET with admin token -> all orders; POST with token -> order has user_email from token; POST without token -> order has body.user_email or null; GET /orders/{id} as user -> 403; as admin -> 200 when exists.
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/test_orders_auth.py -v -x</automated>
  </verify>
  <done>USER-01 and ADMN-01 satisfied: user sees own orders, admin sees all; POST links to JWT user when present; order by-id and mutate admin-only.</done>
</task>

</tasks>

<verification>
- pytest tests/test_names_by_user_auth.py tests/test_orders_auth.py -v passes.
- Unauthenticated GET /orders and GET /names/by-user return 401.
</verification>

<success_criteria>
- get_current_user_optional in deps.py. Names by-user and orders routes guarded and scoped per CONTEXT. Tests cover 401, user scope, admin scope, POST JWT override, admin-only by-id.
</success_criteria>

<output>
After completion, create `.planning/phases/04-protected-routes-and-admin-endpoints/04-02-SUMMARY.md`
</output>
