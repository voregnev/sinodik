[project]
name = "sinodik"
version = "1.0.0"
description = "Синодик — система записок для поминовения"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["app"]
testpaths = ["tests"]
asyncio_mode = "auto"
